<h2>Posts</h2>

<% @posts.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title">
			<%= link_to post.title, post %>
		</h2>
		<p class="date">
			<p>Post created on	<%= post.created_at.strftime('%B, %d, %Y') %> by <%= post.user.name %> </p>
			</p>
	</div>
	
<% end %>